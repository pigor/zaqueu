<div class="container">
  <%= form_for @user, html: {class: "form-signin"} do |f| %>
    <fieldset>
      <legend>Alterar Perfil</legend>
      <div class="form-group">
        <%= f.text_field :name, class: "input-block-level", placeholder: "Digite seu Nome", disabled: "disabled" %>
      </div>
      <div class="form-group">
        <%= f.text_field :username, class: "input-block-level", placeholder: "Digite seu Username", disabled: "disabled" %>
      </div>
      <div class="form-group">
        <%= f.text_field :email, class: "input-block-level", placeholder: "Digite seu Email", disabled: "disabled" %>
      </div>
      <div class="form-group">
        <%= f.password_field :password, class: "input-block-level", placeholder: "Digite sua Senha" %>
      </div>
      <button type="submit" class="btn btn-default btn-block">Salvar</button>
    </fieldset>
  <% end %>
</div>
